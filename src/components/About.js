import React from 'react'

export default function About() {
    
  return (
    <div>
       <div>
      <h1 >About</h1>
      <p>
      Our project name is a notebook. It is built by using the MERN stack. For the frontend we use react JS and for the backend, we use node JS and express JS and for the database, we use MongoDB database in this project user needs to create an account and if an account is already created user can log in using email id and password after that user can add a note, delete note and edit note.
      </p>
    </div>
    </div>
  )
}
